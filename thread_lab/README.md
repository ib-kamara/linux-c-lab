Vocabulary

thread: smallest unit of execution scheduled by the OS. Threads in the same program share memory.

pthreads: the POSIX thread library in C.

pthread_create: starts a new thread running a function you give it.

pthread_join: waits for a thread to finish.

pthread_yield: gives up the CPU so another thread can run.